{
  "name": "parcel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "compile": "sass sassy-css.scss styles.css --watch",
    "start": "parcel index.html",
    "build": "rm -rf dist && parcel build index.html --public-url ./",
    "publish": "gh-pages -d dist"
  },
  "author": "DCIhpf",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  }
}
